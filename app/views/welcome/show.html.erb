<div class="container">
  <br/>
  <!--h1 class="font-bold text-4xl">Challenges#index</h1>
    <div class="flex-1">
      <br/>
      <%#= turbo_frame_tag "invitations" do %>
        <%#= render @invitations, user: current_user %>
      <%# end %>
    </div-->

  <div class="flex justify-between w-full">
    <div class="border">
      <div class="mockup-code" style="background-color: initial">
        <pre data-prefix="$"><code>echo "${greeting}"</code></pre>
        <pre data-prefix=">"><code>👋 Welcome <%= current_user.display_name %>!</code></pre>
        <br/>
        <pre data-prefix="$"><code>tree --challenges</code></pre>
        <pre data-prefix=">"><code></code></pre>
        <div style="margin-left: 3rem; margin-top: -1.4rem">
          <%= turbo_frame_tag "invitations" do %>
            <%= render @invitations, user: current_user %>
          <% end %>
        </div>
        <br/>
        <pre data-prefix="$"><code>man hello_world</code></pre>
        <pre data-prefix=">"><code>NAME
        hello_world -- fun code Challenges

     DESCRIPTION:
        You can now start working on your first challenge.
        Completing all exercises will put you up on the leaderboard!

        Good luck!</code></pre>
        <br/>
        <pre data-prefix="$"><code>help</code></pre>
        <pre data-prefix=">"><code>Please see the <a class="link">FAQ</a> or contact us at <a href="mailto:hello@world.com" class="link">hello@world.com</a>.</code></pre>
        <br/>
        <!--pre data-prefix="$">
        <div style="margin-left: 3rem; margin-top: -7.6rem">
        <%#= simple_form_for :invitation, url: welcome_path, method: :patch, local: true do |f| %>
            <%#= f.input :code, label: false, placeholder: "enter invitation code", input_html: { value: params[:code], class: "input input-sm input-ghost w-full max-w-xs" }  %>
        <%# end %></div></pre-->
      </div>
    </div>
  </div>
</div>
